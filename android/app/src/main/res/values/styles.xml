<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- postSplashScreenTheme: la librería lo aplica apenas termina el splash nativo,
         ANTES de que el WebView haya pintado nada. android:background=@null dejaba
         pasar el windowBackground blanco por defecto de Theme.AppCompat.DayNight en
         ese hueco → frame blanco entre splash nativo y overlay web (#boot-splash).
         windowBackground/statusBar/navigationBar oscuros acá cierran ese hueco. -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowBackground">@color/splash_background</item>
        <item name="android:statusBarColor">@color/splash_background</item>
        <item name="android:navigationBarColor">@color/splash_background</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
    </style>


    <!-- Theme.SplashScreen (androidx.core:core-splashscreen) — API real en Android 12+,
         emulado por la librería en versiones anteriores (ver installSplashScreen() en
         MainActivity.onCreate). El slot de ícono del sistema (windowSplashScreenAnimatedIcon)
         tiene un tamaño acotado por la plataforma — el escudo TSC "grande" real lo muestra
         el overlay web (index.html) apenas pinta el WebView, no este ícono nativo, que es
         solo el puente sin flash blanco entre el lanzamiento y ese primer paint. -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
        <!-- Fallback pre-31 / compat: si el compat theme no cubre algún caso, este
             drawable de fondo completo evita cualquier flash blanco. -->
        <item name="android:windowBackground">@drawable/splash</item>
    </style>
</resources>